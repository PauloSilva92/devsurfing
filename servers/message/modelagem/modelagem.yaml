swagger: '2.0'

# This is your document metadata
info:
  version: "0.0.1"
  title: Devsurfing-messages

# Describe your paths here
paths:
  
  /:message_id:
    
    get:
      
      description: |
        Obtem determinada conversa
      parameters:
        -
          name: message_id
          in: query
          description: o id da conversa
          required: true
          type: string
      responses:
        200:
          description: Resposta de sucesso, devolve um JSon
          
    
  /save:
    put:
      description: |
        Recebe um objeto que com todos os dados para criar ou modificar a conversa
      responses:
        200:
          description: OK
    
      